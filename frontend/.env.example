# Frontend environment variables
# Copy to .env and adjust as needed

# Backend API base URL (preferred)
VITE_API_BASE=http://localhost:4000

# Legacy fallback variable still supported
VITE_API_URL=http://localhost:4000
